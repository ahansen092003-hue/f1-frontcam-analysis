version: '3.8'

services:
  f1-analysis:
    build: .
    image: f1-analysis:latest
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./output:/app/output
    command: ["testvid1.mp4", "0.15"]
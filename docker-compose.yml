version: '3.8'

services:
  f1-analysis:
    build: .
    image: f1-analysis:latest
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./output:/app/output
    environment:
      - YOLO_CONFIG_DIR=/app/.config
    command: ["testvid1.mp4", "0.15"]